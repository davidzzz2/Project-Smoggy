# Smoggy Status — 2026-03-16

## Highlights
- Added a PyMC-based hierarchical logistic baseline (`models/hierarchical_logistic.py`) that matches the blueprint’s Phase 3 requirements.
- Captured modeling dependencies in `configs/requirements.txt` to unblock environment creation.
- Published an example YAML config (`models/configs/model1_example.yaml`) so everyone can align on column names, priors, and sampling parameters.
- Updated `model_blueprint.md` with the implementation status and refreshed next steps.

## Instructions
1. Drop your Phase 2/3 feature parquet into `data_docs/phase2/` (or point to any path) and run:
   ```bash
   uv run python Smoggy/models/hierarchical_logistic.py fit \
       --config Smoggy/models/configs/model1_example.yaml \
       --features /path/to/features.parquet \
       --output-dir artifacts/model1
   ```
2. Inspect `artifacts/model1/posterior_summary.csv` plus `loo.json` for convergence + generalization checks.
3. Use `predict` subcommand to score held-out cohorts once the posterior is frozen.

## Next Up
- Plug in Niko’s finalized feature dictionary and create canonical parquet schemas.
- Stand up benchmarking scripts (Elastic Net + XGBoost) under `evaluation/` for calibration comparisons.
- Automate periodic status pushes (GitHub Actions or cron) so we never miss the twice-daily update window again.
